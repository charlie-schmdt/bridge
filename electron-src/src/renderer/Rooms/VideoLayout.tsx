import { Card, CardBody } from "@heroui/react";
import Header from '../components/Header';

export const VideoLayout = () => {
    return (
        <Card>
            <CardBody>
                <Header />
                {/*<VideoFeed />*/}
            </CardBody>
        </Card>
    );
}
